llm:
  tools:
    hermes:
      model:
        - xai-oauth/grok-4.6
      reasoning_effort: high
      providers:
        # Hermes owns this provider's OAuth flow and bundled model catalog.
        # models: false keeps llm/tool-config from trying to enumerate it.
        xai-oauth:
          provider: xai-oauth
          models: false

# MSYS2 pacman. `msys` is POSIX tools under /usr. `ucrt64` names are
# prefixed with mingw-w64-ucrt-x86_64- when installing.
[packages.msys2]
msys = [
  "curl",
  "git",
  "jq",
  "unzip",
  "zsh",
]
ucrt64 = [
  "nodejs",
]

{{- $pass := ternary "Work/IV/LLM" "" (eq .host_env "iv") -}}
{{- $fields := passFields $pass -}}

{{- dict
  "anthropic" (dict
    "type" "api"
	"key" (index $fields "anthropic-auth-token")
  )
  | toPrettyJson
-}}

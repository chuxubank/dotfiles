{{- $fields := passFields "Work/IV/LLM" -}}

{{- dict
  "iv" (dict
    "type" "api"
	"key" (index $fields "anthropic-auth-token")
  )
  "anthropic" (dict
    "type" "api"
	"key" (index $fields "cc-auth-token")
  )
  | toPrettyJson
-}}

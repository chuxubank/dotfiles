#!/bin/bash

{{ $sdkmanDir := joinPath .chezmoi.homeDir .path.sdkman }}

echo "{{ template "week" . }} 󰯁 Setup sdkman"

{{- if not (stat $sdkmanDir) }}
curl -s "https://get.sdkman.io" | bash
{{- end }}

source {{ joinPath $sdkmanDir "bin" "sdkman-init.sh" }}

sdk selfupdate
